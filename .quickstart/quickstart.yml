database_key: claude_database
schema_key: claude_schema

dbt_versions: ">=1.3.0 <3.0.0"

table_variables:

destination_configurations:
  databricks:
    dispatch:
      - macro_namespace: dbt_utils
        search_order: [ 'spark_utils', 'dbt_utils' ]

public_models: [

]

supported_vars: